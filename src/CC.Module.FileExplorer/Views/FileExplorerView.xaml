<UserControl x:Class="CC.Module.FileExplorer.Views.FileExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:CC.Module.FileExplorer.Views">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <views:FileTreeView Grid.Column="0" x:Name="FileTreeViewLeft" />

        <views:FileTreeView Grid.Column="1" x:Name="FileTreeViewRight" />
    </Grid>
</UserControl>
